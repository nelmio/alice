framework:
    secret:                  NelmioAliceBundleSecret
    router:
        resource:            ~
        strict_requirements: '%kernel.debug%'
    trusted_proxies:         ~
    test:                    ~
    session:
        storage_id:          session.storage.mock_file
    property_access:
        magic_call:          true
